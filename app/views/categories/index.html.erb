<h1>カテゴリー一覧</h1>
<p><%= link_to "カテゴリ新規登録", [:new, :category] %></p>
<%= render "menu" %>
<table class="categories">
	<col class="name">
	<col class="links">
	<% @categories.each do |category| %>
	<tr>
		<td><%= category.name %></td>
		<td><%= links_for(category) %></td>
	</tr>
	<% end %>
</table>