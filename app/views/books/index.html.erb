<table class="books">
<col class="title">
<col class="authors">
<col class="publish_year">
<% @books.each do |book| %>
	<tr>
		<td>タイトル： <%= link_to book.title, book%>: <%= link_to "編集", [:edit, book]%></td>
		<td>筆者：<%= book.authors %></td>
		<td>出版日：<%= book.publish_year %></td>
	</tr>
<% end %>
<td>
</table>

<h3><%= link_to "新規登録", [:new, :book]%></h3>